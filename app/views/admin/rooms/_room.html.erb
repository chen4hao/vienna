<tr>
  <td><%= room.no %></td>
  <td><%= room.name %></td>
  <td><%= room.list_price %></td>
  <td><%= room.holiday_price %></td>
  <td><%= room.hotday_price %></td>
  <td><%= room.weekday_price %></td>
  <td><%= room.add_bed_fee %></td>

  <td>
    <%= link_to("編輯", edit_admin_room_path(room), class: "btn btn-primary")%>
    <%= link_to("刪除", admin_room_path(room), class: "btn btn-danger",
                  method: :delete, data: { confirm: "你確定要刪除嗎?" } )%>
  </td>
</tr>
